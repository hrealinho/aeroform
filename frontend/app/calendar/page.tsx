export default function Page(){return <><h1>Calendar</h1><p className="muted">Planned and completed sessions will share one working calendar.</p><div className="card section"><h2>Planning vertical slice</h2><p>Create planned workouts through <code>POST /api/v1/planned-workouts</code>. The API already returns planned and actual sessions through <code>/calendar</code>. Drag-and-drop UI and automatic matching are the next implementation step.</p></div></>}
