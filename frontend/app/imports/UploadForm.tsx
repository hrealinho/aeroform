"use client";import {useState} from "react";import {API} from "@/lib/api";
export default function UploadForm(){const [status,setStatus]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);setStatus("Importing...");const r=await fetch(API+"/imports/files",{method:"POST",body:fd});const j=await r.json();setStatus(r.ok?`Done: ${j.imported_count} imported, ${j.duplicate_count} duplicates, ${j.failed_count} failed`:`Failed: ${JSON.stringify(j)}`)}return <form className="card section" onSubmit={submit}><h2>Import activity history</h2><p className="muted">Upload FIT, GPX, TCX, or a ZIP archive containing thousands of activities.</p><div className="row"><input className="input" type="file" name="files" multiple accept=".fit,.gpx,.tcx,.zip"/><button className="button">Upload</button></div>{status&&<p>{status}</p>}</form>}
